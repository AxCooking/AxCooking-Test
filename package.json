{
  "name": "axcooking-image-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "optimize": "node scripts/optimize-images.mjs",
    "optimize:force": "node scripts/optimize-images.mjs --force",
    "optimize:cleanup": "node scripts/optimize-images.mjs --cleanup-only"
  },
  "dependencies": {
    "p-limit": "^6.2.0",
    "sharp": "0.34.5"
  }
}
